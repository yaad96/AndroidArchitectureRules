{
  "name": "android-architecture-rules",
  "version": "1.0.0",
  "private": true,
  "description": "Android architectural design-rule fixtures for the DesignFix tool, plus an offline compile gate.",
  "scripts": {
    "compile": "node test/compile-check.mjs",
    "compile-strict": "node test/compile-check.mjs --strict"
  }
}
